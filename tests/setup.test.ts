import { describe, it, expect } from "vitest";

describe("project setup", () => {
  it("TypeScript types compile", () => {
    // If this file compiles, TS config is valid
    expect(true).toBe(true);
  });
});
